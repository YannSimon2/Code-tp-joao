import numpy as np
import matplotlib.pyplot as plt
from scipy.constants import c

needle_size_px =
u_needle_size_px=
needle_size_m = 1.20e-3
u_needle_size_m = 0.02e-3


plasma_size_px = np.array([])

plasma_size_m= np.array([])

delays_m = np.array([])

delays_s = np.array([])/c